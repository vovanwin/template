version: v2
deps:
  - buf.build/cludden/protoc-gen-go-temporal

lint:
  use:
    - STANDARD
    - COMMENT_ENUM
    - COMMENT_MESSAGE
  ignore:
    # Исключаем внешние proto-файлы из линтинга
    - google
    - buf
